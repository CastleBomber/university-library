nano ~/.bashrc
source ~/.bashrc
used to force a reload and allow for nvm

nvm --version

nano ~/.bash_profile
cat ~/.bash_profile

nvm install 18
nvm alias default 18

source ~/.bash_profile

nvm --version && node -v && which node